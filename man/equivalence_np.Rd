% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/equivalence_np.R
\name{equivalence_np}
\alias{equivalence_np}
\title{Equivalence / non-inferiority on probabilistic nonparametric effects}
\usage{
equivalence_np(
  x,
  g,
  delta,
  measure = c("auc", "cliff"),
  alternative = c("equivalence", "noninferiority", "noninferiority_rev"),
  level = 0.95,
  nboot = 2000,
  seed = NULL
)
}
\arguments{
\item{x}{Numeric vector; @param g group labels (same length as x).}

\item{g}{Factor/character group labels (same length as x).}

\item{delta}{Non-negative margin.}

\item{measure}{"auc" or "cliff".}

\item{alternative}{"equivalence","noninferiority","noninferiority_rev".}

\item{level}{CI level; @param nboot bootstrap reps; @param seed optional seed.}

\item{nboot}{Integer number of bootstrap resamples for the CI.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
data.frame with: group1, group2, measure, effect, ci_low, ci_high,
center, delta, alternative, decision
}
\description{
Uses bootstrap CIs for AUC or Cliff's delta to decide equivalence / non-inferiority
for all group pairs. "No-difference" center: 0.5 (AUC) or 0 (Cliff).
}
\examples{
set.seed(2)
x <- c(rnorm(15,0), rnorm(15,0.05)); g <- factor(rep(c("A","B"), each=15))
equivalence_np(x, g, delta = 0.05, measure = "auc",
               alternative = "equivalence", nboot = 200, seed = 1)
}
